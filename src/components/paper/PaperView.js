import React from 'react';
// import PropTypes from 'prop-types';

const PaperView = () => <canvas id="myCanvas" resize="true" style={{ width: '100vw', height: '100vh' }} />;

PaperView.propTypes = {};

export default PaperView;
